/**
 * Default environment - used by `ng serve` / development builds.
 * Production values live in `environment.production.ts` and are swapped in
 * via the `fileReplacements` build configuration in angular.json.
 */
export const environment = {
  production: false,
  appName: 'Kapis Clinic AI',
  apiBaseUrl: 'http://localhost:3000/api',
};
