/** Keys used for browser `localStorage` access. Centralized to avoid typo'd string literals. */
export const STORAGE_KEYS = {
  AUTH_TOKEN: 'kapis_auth_token',
  AUTH_USER: 'kapis_auth_user',
} as const;
